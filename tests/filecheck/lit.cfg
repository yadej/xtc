import lit.formats
import os

config.name = "xtc"
config.test_format = lit.formats.ShTest()
config.suffixes = ['.mlir', '.py']

config.test_source_root = os.path.dirname(__file__)
config.excludes = ['experimental','issues']
config.environment["TIMEOUT"] = "10"
